# Tutorials

Task-oriented how-tos for writing Typst documents that evaluate Rust through `evcxr-typst`. Each file is one task.

For the design of the tool itself, see `docs/design/`. For the raw working notes that often motivated these tutorials, see `journal/`.

## Index

(Add a one-line entry here when you add a tutorial.)

- [`first-chapter.md`](first-chapter.md) — write your first evcxr-typst document end to end.
