# CLAUDE.md — `packages/`

Typst packages. Currently holds the single user-facing package `evcxr/`. Per-package guidance is in each package's own `CLAUDE.md`.

The user-facing API contract lives here. Renames and signature changes are governed by `../docs/DECISIONS.md` (D-012 names, D-019 schema versioning).
