# `flow_control/match` chapter

**Date:** 2026-05-15
**Branch:** rbe/flow-control-match
**Upstream source:** `.rust-by-example/src/flow_control/match.md` (snapshot 898f0ac)

## What I tried

Port the `match` chapter — the parent entry under `flow_control` before the destructuring/guard/binding sub-chapters. Single snippet upstream, wrapped in `fn main()`. Two distinct demos in one body: (a) `match number { 1 | 2|3|5|7|11 | 13..=19 | _ => ... }` exercising single value, alternation, inclusive-range, and catch-all; (b) `let binary = match boolean { false => 0, true => 1 };` showing match as an expression. Used `rust-main(...)`.

## What happened

Watch loop evaluated cleanly. Sidecar matches upstream exactly:

```
Tell me about 13
A teen
true -> 1
```

No `WARN evcxr_typst` lines. `typst compile --root . main.typ` exits 0 (only the pre-existing unrelated monospace fallback warning).

## What I learned

Nothing new — this chapter is a "clean port" with no quirks. Inclusive-range patterns (`13..=19`), alternation patterns (`2 | 3 | 5 | 7 | 11`), and match-as-expression all pass through `rust-main` and evcxr without special handling. Worth noting as a counterpoint to the if-let / while-let chapters which both had top-level inference issues — `match` here is inside `fn main()` so the same trap is avoided.

## Follow-ups

- [ ] Open: `flow_control/match/destructuring.md` is next (parent of `destructure_tuple/slice/enum/pointers/structures`). It's a thin intro chapter; the substance is in the sub-chapters. Pick up `destructuring.md` next.
