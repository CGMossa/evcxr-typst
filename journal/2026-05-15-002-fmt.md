# `hello/print/fmt` chapter (Formatting)

**Date:** 2026-05-15
**Branch:** rbe/fmt-chapter
**Upstream source:** `.rust-by-example/src/hello/print/fmt.md` (snapshot 898f0ac)

## What I tried

Port the chapter sibling after `print_display/`: `hello/print/fmt.md`, which closes out the formatted-print subtree. Single `evcxr.rust-main(...)` snippet defining `struct City` (with a manual `Display` impl) and `struct Color` (with `#[derive(Debug)]`), and a `main` that loops over a hard-coded array of each.

## What happened

Straightforward — no top-level item collisions with prior chapters (`City` and `Color` are new), no syntax beyond what `print_debug`/`print_display` already exercised. The chapter prose opens with three `format!(…)` *prose* bullets demonstrating `{}` vs `{:X}` vs `{:o}` on a placeholder `foo`; left as plain Typst text because they're illustrative and there's no defined `foo` to evaluate against.

Bare `typst compile --root . examples/rust-by-example/main.typ` succeeds; PDF grew 237 → 286 KB matching the chapter content.

The activity (implement `Display` for `Color` so it prints `RGB (r, g, b) 0xRRGGBB`) is left as prose, per the convention pinned by `examples/rust-by-example/CLAUDE.md`.

## What I learned

Print-trait chapters are now a tight pattern: one rust-main per chapter, names that don't collide across chapters, activities kept as prose. Subsequent chapters (`primitives.md` onward) will exercise different surface (literals, tuples, arrays) and likely shift the snippet density.

## Follow-ups

- [ ] Integration eval (`evcxr-typst run --allow-eval --root . examples/rust-by-example/main.typ`) deferred per the testcase-list journal entry's open follow-up. Both new snippets (`rbe-hello-print-display-testcase-list`, `rbe-hello-print-fmt`) should land in the cache when run from a fresh terminal.
