# `custom_types/enum/c_like` chapter

**Date:** 2026-05-15
**Branch:** rbe/enum-c-like
**Upstream source:** `.rust-by-example/src/custom_types/enum/c_like.md` (snapshot 898f0ac)

## What I tried

Port the C-like enums subchapter. Single `rust-main` snippet with two enums and `as i32`/`as u32` discriminant casts.

`Color` here redefines the prior `struct Color` (from `hello/print/fmt`). The kind changes too (struct → enum). evcxr handled the type redefinition silently (no live binding from fmt's `fn main` referenced the prior struct).

## What happened

Output matches upstream verbatim:

```
zero is 0
one is 1
roses are #ff0000
violets are #0000ff
```

## Follow-ups

- `Color` will be redefined again in `flow_control/match/destructuring/destructure_enum.md` later in the book — same kind (enum), different variants. Worth a note in that future port's journal entry that this kind of cascading redefinition is expected.
