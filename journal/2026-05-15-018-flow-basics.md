# `flow_control` basics — opener + if_else + while + loop

**Date:** 2026-05-15
**Branch:** rbe/flow-basics
**Upstream source:** `.rust-by-example/src/flow_control{.md,/if_else.md,/while.md,/loop.md}` (snapshot 898f0ac)

## What I tried

Open the Flow of Control section with four chapters: opener (prose), `if_else.md` (branching + branch-as-expression), `while.md` (FizzBuzz to 100), `loop.md` (counter to 5 with `continue` and `break`).

All three runnable snippets use `rust-main`. The `while` FizzBuzz prints 100 lines — biggest sidecar in the book so far.

## What happened

Watch eval matched upstream for all three. Spot-checks:

```
5 is positive, and is a small number, increase ten-fold
5 -> 50
```
```
Let's count until infinity!
1
2
three
4
5
OK, that's enough
```

And 100 lines of FizzBuzz that matches upstream verbatim (`1`, `2`, `fizz`, `4`, `buzz`, …, `100`).

## Follow-ups

- None.
