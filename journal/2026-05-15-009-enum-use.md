# `custom_types/enum/enum_use` chapter

**Date:** 2026-05-15
**Branch:** rbe/enum-use
**Upstream source:** `.rust-by-example/src/custom_types/enum/enum_use.md` (snapshot 898f0ac)

## What I tried

Port the `use` subchapter of Enums. Single `rust-main` snippet defining `enum Stage` and `enum Role` at top level (both names unique to this chapter per `grep -rn '^enum Stage\|^enum Role'`).

## What happened

Mechanical — watch loop evaluated, sidecar matches upstream:

```
Beginners are starting their learning journey!
Students are acquiring knowledge!
```

## What I learned

Nothing new; pattern is now boring (in the good way).

## Follow-ups

- None.
