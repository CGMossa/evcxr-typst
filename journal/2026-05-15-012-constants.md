# `custom_types/constants` chapter

**Date:** 2026-05-15
**Branch:** rbe/constants
**Upstream source:** `.rust-by-example/src/custom_types/constants.md` (snapshot 898f0ac)

## What I tried

Port the constants chapter, last child of `custom_types/`. Upstream tags the block `ignore,mdbook-runnable` because the snippet has a deliberate compile error (`THRESHOLD = 5;` after declaring `const THRESHOLD: i32 = 10;` — the chapter is teaching "you can't mutate a const").

Same pattern as `primitives.md` (commit `1ba0906` originally): render source-only to preserve the pedagogical demonstration. Evaluating would replace the chapter's "look, can't do this" with our compile-error box.

## What I learned

Nothing new; this is now the third source-only chapter (`primitives.md`, `primitives.typ`'s definition-only-blocks, and now this one). The `ignore` semantics divide cleanly into two categories:

- **Pedagogical compile error** (this one, `primitives.md`): source-only.
- **Defensive ignore for commented-out panic lines** (`primitives/array.md`): runs cleanly when the un-commented body is sent.

## Follow-ups

- Time to fold this into `examples/rust-by-example/CLAUDE.md` § "Fidelity vs. evcxr's evaluation model"? The pattern is stable across three chapters now. Tracked as the running follow-up from `2026-05-15-003-primitives.md`. Could batch with the next round of CLAUDE.md updates.
