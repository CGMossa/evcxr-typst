# `flow_control/for` chapter

**Date:** 2026-05-15
**Branch:** rbe/flow-for
**Upstream source:** `.rust-by-example/src/flow_control/for.md` (snapshot 898f0ac)

## What I tried

Port the for-loop chapter. Seven snippets upstream:

- Exclusive-range FizzBuzz (`1..101`) — `rust-main`.
- Inclusive-range FizzBuzz (`1..=100`) — `rust-main`.
- Empty-range demo (`10..1`) — bare statements, no `fn main`. Source-only.
- Reverse-range demo (`(1..10).rev()`) — bare statements, no `fn main`. Source-only.
- `iter` demo — `rust-main`.
- `into_iter` demo — `rust-main`.
- `iter_mut` demo — `rust-main`.

## What happened

All five runnable snippets evaluated, output matched upstream verbatim. Spot-checks:

- `iter`: `Hello Bob` / `Hello Frank` / `There is a rustacean among us!` / `names: ["Bob", "Frank", "Ferris"]`
- `iter_mut`: `names: ["Hello", "Hello", "There is a rustacean among us!"]`

Both FizzBuzz variants produce 100 lines (matching upstream).

## Follow-ups

- None.
